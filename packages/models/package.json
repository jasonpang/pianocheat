{
  "name": "models",
  "version": "1.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "test": "node --test --watch",
    "clean": "rm -rf ./build; rm -rf ./node_modules"
  },
  "dependencies": {
    "tsconfig": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.7",
    "typescript": "^5.3.3"
  }
}
